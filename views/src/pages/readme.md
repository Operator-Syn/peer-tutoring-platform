# 🗂 React Pages

What is it?
- Each file in this folder represents a page in the app (e.g. Home, Login, Dashboard).
- Pages are composed of multiple React Components imported from the /components directory.
- Used by the router to define app navigation (e.g. React Router).